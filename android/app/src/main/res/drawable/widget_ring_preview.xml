<?xml version="1.0" encoding="utf-8"?>
<!-- Static stand-in for the ring the service draws at runtime, so the widget
     picker has something to show. Never used by a placed widget. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <stroke
        android:width="4dp"
        android:color="#FF6DE86D" />
</shape>
